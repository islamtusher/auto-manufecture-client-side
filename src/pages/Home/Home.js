import React from 'react';
import HeroArea from '../HomeComponents/HeroArea';

const Home = () => {
    return (
        <div>
            {/* <HeroArea></HeroArea> */}
        </div>
    );
};

export default Home;